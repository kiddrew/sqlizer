<%= form_for(@query_token) do |f| %>
  <% if @query_token.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@query_token.errors.count, "error") %> prohibited this query_token from being saved:</h2>

      <ul>
      <% @query_token.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
